using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BloodSplash : MonoBehaviour
{
    gunScript rayHitScript;
    // Start is called before the first frame update
    void Start()
    {
            
    }

    // Update is called once per frame
    void Update()
    {
        //if (rayHitScript.rayHit.collider.gameObject.CompareTag("Enemy"))
        //{
        //    Debug.Log("Enter in rayHit");
        //    //bloodSplashEffect.Play();
        //    Instantiate(bloodSplashEffect, rayHitScript.rayHit.point, Quaternion.identity);
        //}
    }

}
